en:
  field_enforce_issue: Enforce issue id in time entry
  activerecord:
    errors:
      models:
        time_entry:
          attributes:
            hours:
              too_long: "You can report maximum 20h per day"
              too_long_limit: "You can report maximum 20h per day. Already reported %{limit} in %{day}"
            spent_on:
              too_many_days: "You can report maximum %{limit} days back"
